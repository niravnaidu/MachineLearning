#How to find square root 2
sqrt(5000)

is.numeric("A")

as.logical(0)

#as. changes it to something (not sure)

# table(x) is a frequency table
# hist() is a histogram
# plot() is a barplot

# assoc(y~x)
# use data() to load data from datasets that are already in R
